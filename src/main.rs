//! mentci-egui — entry point.
//!
//! Constructs the WorkbenchState, opens daemon connections,
//! runs the eframe event loop. Everything else lives in
//! [`app`] and [`render`].

mod app;
mod error;
mod render;

fn main() -> eframe::Result<()> {
    todo!()
}
